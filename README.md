# Human Tornado

## Building and running on localhost

First install dependencies:

```sh
yarn install
```

To start dev server:

```sh
yarn start
```

To create a production build:

```sh
yarn build
```

To clear cache:

```sh
yarn clear
```
